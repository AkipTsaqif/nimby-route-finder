# NIMBY Route Finder - Environment Configuration
# Copy this to .env and fill in your values

# Set to "false" to use real elevation data instead of synthetic terrain
DEMO_MODE=true

# Mapbox API token (get one free at https://account.mapbox.com/)
# Required if you want to use Mapbox Terrain-RGB as elevation source
MAPBOX_ACCESS_TOKEN=

# Path to local DEM folder (optional, defaults to ./data/dem)
# DEM_FOLDER=./data/dem
